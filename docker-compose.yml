
services:
  vite-app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://127.0.0.1:8000
      - CHOKIDAR_USEPOLLING=true
